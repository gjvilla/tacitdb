# Tacit — The Four-Rooms Register

The project's map of its own knowledge, kept in the discipline it exists to serve:
known knowns, known unknowns, unknown knowns, unknown unknowns. The register is a
working document with a cadence, not a filing cabinet — it leaks by default and is
re-read on a schedule (see the practices section).

---

## Room 1 · Known knowns

What the project has decided and recorded, with owners and review triggers:

- **[DECISIONS.md](DECISIONS.md)** — eleven founding decisions plus the dated
  success hypothesis H-0001. Each carries its own review trigger.
- **[REQUIREMENTS.md](REQUIREMENTS.md)** — eleven requirements from production scar
  tissue, each with an acceptance criterion, plus the shed list.
- **[priors/](priors/)** — the prior-art survey (one page per engine), filled by
  research as of 2026-08-23. Verdict in [priors/SUMMARY.md](priors/SUMMARY.md).
- **[design/001-data-model.md](design/001-data-model.md)** — the logical data
  model: two ledgers, envelope spec, lifecycle invariants (resolves U-1),
  bitemporality, projected graph, retrieval semantics. Promoted 2026-08-23
  (D-0012, D-0013, D-0014).
- **`crates/`** — the workspace (2026-08-23, per D-0015 and D-0017):
  `tacit-core` (the data model as types — sealed records, derived state,
  verdict grammar, instrument panel, and the projected graph — with the
  invariant suite and U-10 property tests), `tacit-keeper` (corpus parser and
  the `docs/DECISIONS.md` ingest, plus the `dogfood` example),
  `tacit-mcp` (the MCP host: ten typed, audited tools over stdio, per D-0018),
  `tacit-python` (binding shell, no PyO3 yet).
- **And a second corpus it did not write** (2026-08-24, D-0030).
  `cargo run --release -p tacit-keeper --example scale -- 32000` generates a
  corpus of any size with its ground truth, in pseudo-words no decision record
  can contain, and reports what the engine costs over it. The separation from
  the self-hosting corpus is structural rather than a discipline someone has to
  keep.
- **The corpus ingests itself.** `cargo run -p tacit-keeper --example dogfood`
  loads this project's own decision records *and this register* into the engine
  and interrogates them: provenance with evidence chains, record-time travel,
  the projected graph, and weighted paths over the instrument panel. It prints
  its own honest position against H-0001(a).
- **[GOLDEN.md](GOLDEN.md)** — the golden suite, which now audits itself three
  ways (2026-08-24, D-0028, D-0029, D-0037): the build fails when a question
  rests on a review trigger that has already fired, when the corpus quotes a
  question back at it, and when the corpus acquires a word a question was agreed
  against. Four fired triggers and two quotes had accumulated unnoticed, one
  question was passing on two errors cancelling, and one was lifted from failing
  to passing by two words of prose in a decision record. `cargo run -p tacit-keeper --example
  explain` answers the question the suite cannot — *why* a ranking came out as
  it did.
- **[GOLDEN.md](GOLDEN.md)** — the golden suite: representative questions with
  agreed answers, graded by `cargo run -p tacit-keeper --example golden`. It
  scores abstention as a pass and classifies every failure by which room it
  came from, so "retrieval missed what the record holds" and "retrieval
  answered what it does not" never collapse into one accuracy number. Known
  shortfalls must name a registered unknown; a failure with no registered
  cause turns the suite red and fails the build.
- **A suite over words nobody here wrote** (2026-08-30, D-0039).
  [PEP-GOLDEN.md](PEP-GOLDEN.md) — twenty-four questions agreed against sixty
  pinned packaging proposals, graded by `cargo run -p tacit-keeper --example
  pep_golden` after `scripts/fetch-proposals.sh`. The runner refuses a corpus
  that is not exactly the pinned slice. First grading 16/24; every shortfall
  filed by measurement (`explain --proposals`), two new unknowns registered
  (U-40, U-41), and one ingest fault found by a question designed to catch it.
- **Trust is asked twice** (2026-08-24, D-0027). What the verdict recorded stays
  as it was; `review_trust` re-asks the repository on demand, and the host raises
  the alarm on the way up. A weakening changes nothing in the record — retiring
  what a revoked key signed is a person's verdict to declare.
- **Whose signature counts** (2026-08-24, D-0026). Only a key this machine's git
  trusts, and only an identity the signature binds — named with `--signed-by`
  from outside the repository, because a list of who may promote cannot live in
  the file it protects.
- **A transcribed verdict says who wrote the prose** (2026-08-24, D-0025).
  `cargo run -p tacit-mcp -- --require-signature .` declines to promote on words
  no signed commit carries; the default records what git can establish and says
  so in the verdict itself, so "which promotions rest on nothing" stays
  answerable long after the ingest that made them.
- **The inbox shows one wording per record** (2026-08-24, D-0024). A draft its
  author already replaced keeps its state and loses its separate place in the
  queue; the count of what was folded comes back beside the list, because a
  queue that quietly drops records is how a reviewer comes to believe they have
  seen everything.
- **A withdrawn question says which kind of withdrawal it was** (2026-08-24,
  D-0023). Reworded questions supersede; the four reasons keep "we asked it
  better" apart from "we stopped asking", and keep "the answer is somewhere
  else" from disappearing into either.
- **The documents stay upstream** (2026-08-24, D-0021). Re-reading them is a
  sync: unchanged records write nothing, edited ones supersede what they
  replace and retire it in the same verdict, and the three things an ingest may
  not decide on its own — a deleted record, a reworded question, a claim a
  person already retired — are reported rather than performed.
- **The record survives the process** (2026-08-23, D-0019). `cargo run -p
  tacit-mcp -- --store <path> .` keeps the ledger on disk, so what an agent
  proposes is still waiting for a person on the next run. Loading replays the
  log through the grammar rather than deserializing it, so a hand-edited store
  cannot smuggle in a promotion.
- **Agents can reach the record** (2026-08-23). `cargo run -p tacit-mcp -- .`
  serves this repository's corpus over MCP. The ratchet is visible in the tool
  surface as an absence: there is no promote tool, so no sequence of calls an
  agent can make moves a claim to promoted. An integration test asserts that.
- **Room 2 is in the ledger, not only in this file** (2026-08-23). Every row
  below is ingested as a gap record carrying its trigger, so the engine can
  answer "that is a registered open question" instead of "nothing found" —
  the raw material for honest abstention. Resolved rows are transcribed as
  `Answer` verdicts naming the promoted claim that settled them, which the
  engine refuses unless that claim really is promoted. Consequence worth
  knowing: this document is now load-bearing for the test suite. A malformed
  row is a hard error, by design.

A known known is only as good as its review trigger — see Room 4 for the backward
door.

## Room 2 · Known unknowns

Named questions without agreed answers. Each has a trigger — the event that forces
the decision — because an unregistered gap is how systems bluff.

| id | Question | Trigger | Notes |
|----|----------|---------|-------|
| U-1 | ~~Write-path placement~~ **Resolved 2026-08-23** → D-0012: grammar in the engine (invariants in design/001 §3.2), identity/auth in the keeper | — | Storage code unblocked. Kept for the record; history is never rewritten. |
| U-2 | ~~Runtime shape~~ **Resolved 2026-08-23** → D-0015: embedded-first Rust library + Python bindings; an MCP host binary is the only served surface in v1 | — | Multi-app serving reopens later as a keeper-layer wrapper around the same core (see D-0015's trigger). Kept for the record. |
| U-3 | ~~Query language: whether, when, and what shape~~ **Resolved 2026-08-31** → D-0049: no — decided on the trigger's own terms. The audit was made durable so usage could be observed at all, a real agent session then drove all ten tools with every call recorded, and every friction found decomposed into a typed parameter or a bound, none into a grammar | — | The trigger was unfireable as built — the audit died with the host process, so usage could happen and never be observed; that fix (the audit persists beside a durable store) is half the resolution. The observed fixes landed with it: the open-questions listing takes a query ranked by the search's own gap rule, both enumerations take limits and still report true totals, and as-of publishes the valid-time answer beside record time. The chains earned no composition — each step is a choice point where the agent reads before deciding. What was imagined but not reached for (an entity-centric read) stays unbuilt for this row's own reason: evidence, not taste. The audits now accumulate; a differently-shaped workload re-reads this against them. |
| U-4 | Which graph algorithms live in-engine | Data-model doc + first retrieval implementation | Weighted Dijkstra/Yen's almost certainly (R-5); community detection / centrality unclear. |
| U-5 | ~~Storage layer~~ **Resolved 2026-08-23** → D-0019: an append-only JSON-lines event log, fsynced before the in-memory commit, replayed through the same validation an append runs. No external storage dependency. | — | The event-log candidate won. The load path was the real question, not the format: a store that is re-validated rather than trusted is what keeps the invariants true of records that came off disk. Residual costs are U-24 and U-25. |
| U-6 | Name: trademark counsel review of the live TACIT Class-9 mark before commercial use | Before any commercial use; re-read after 2026-11-12, when the closest registration either shows a maintenance filing or has cancelled on its own | Narrowed 2026-08-23: registries verified — bare `tacit` taken everywhere, **`tacitdb` clean** (crates.io/GitHub/PyPI; domains likely unregistered, confirm at registrar). Registrable identity `tacitdb`, product name Tacit (D-0011). Counsel item remains. **Narrowed again 2026-09-03, by reading the filings this row had only counted.** The "two pending 2025 applications" were one: Reflex Technologies (serial 99137814, filed 2025-04-15, allowed 2026-05-26, no statement of use yet) claims Class 9 for silent-speech wearables and the software embedded in them — an API and development tools among the goods, each qualified by that field — and the second was a 2026 filing for printed planners. Nothing has ever been filed for `tacitdb`; the bare word sits on 127 marks across unrelated fields. The live registration this row was written about, TacitWear's 6053870, is the closest in goods — its claim includes "software for creating searchable databases" — and is the one that may not survive: its six-year declaration of continued use fell due 2026-05-12, the freshest data reachable shows the office's reminder and no filing after it, and the company's own domain is on a registrar's auction block. The grace period ends 2026-11-12; without a filing the registration cancels on its own. Two facts stayed out of reach and are named so they are checked rather than assumed: whether that declaration was filed after May 2025, and what the office action of 2025-10-28 against Reflex cited — if it named TacitWear and was overcome, that is the office's own reading of how far the word stretches inside Class 9. Both are a minute on TSDR, which would not render without an interactive session. The counsel item stands; what it would now be asked is narrower than it was. |
| U-7 | ~~IP clarity: does the employment agreement's invention-assignment clause reach this project?~~ **Resolved 2026-08-29** → D-0038: no employment agreement carrying an invention-assignment clause exists — none was ever signed, so there is no clause to reach anything. With ownership no longer open, the authorship record was corrected: all commits restamped to the personal identity via `git filter-repo --mailmap`, the pre-rewrite history preserved in a mirror clone, timestamps left untouched. | If the employer later introduces an IP agreement, or asserts any claim; counsel confirmation before commercial use (joins U-6's counsel item) | The refusal to rewrite (D-0035) was conditional on ownership being open; the resolution was recorded and committed *before* the rewrite, so the record shows the order. Residual: this is a factual resolution, not a legal opinion — jurisdictional default doctrines unexamined. History of the row: **Sharpened 2026-08-24 (D-0035)** — the project's own checks found every commit authored and signed under an employer email address, recorded rather than tidied while the question was open. |
| U-8 | Which layer has product pull: engine or keeper | First external adoption/interest signal | The two-layer bet (D-0002) defers this deliberately. **Made observable, 2026-08-31.** The trigger could not fire against a private repository with no front door: a README now exists, the pre-publication scrub runs clean, and publication waits on exactly the two decisions this register cannot make — U-6's counsel item and U-17's license. What will count as signal is agreed now, so it is read then rather than argued then: engine pull is questions and patches about the library surface (the grammar, retrieval, the bindings); keeper pull is questions about the discipline (the register format, the golden practice, the host's workflows) — and the MCP audit files (D-0049) record the second kind directly. A star count is not a signal; a question someone needed answered is. |
| U-9 | ~~Seed corpus beyond self-hosting~~ **Narrowed 2026-08-24** → D-0030 · **Resolved 2026-08-30** → D-0039: sixty packaging proposals, pinned by commit and fetched by script, with twenty-four questions agreed against them in docs/PEP-GOLDEN.md | — | The synthetic half is done and it earned its keep immediately — it found an O(n) lookup inside a sort comparator that was invisible at fifty-four records, and turned U-25 and U-26 from adjectives into numbers. What stays open is the *real-language* half: synthetic prose has no paraphrase, no dialect and no jargon drift, so it cannot grade the thing U-23 is about. A public corpus of published proposals with authors, dates, statuses and supersessions is the shape; vendoring one raises licensing and repository weight, and D-0010 rules out the obvious private alternative. **Narrowed 2026-08-29.** The candidate is the Python enhancement proposals, dual public-domain and CC0-1.0, whose headers carry author, dates, a status over nine values, and supersession both ways — the model as metadata rather than prose convention. The work is a third producer shaped like `synthetic.rs`, writing records straight to its own ledger, with no change to the core and no use of the strict parser. It must be a separate ledger for the reason D-0030 separated the generated one, and that separation is what ends the bind of 2026-08-29: a suite agreed against an outside ledger cannot be moved by prose written into this file. Two costs not named before — it fires U-11, whose trigger is any external or personal-data corpus, because the authors are real people; and the expense is agreeing a question set by hand, which nothing generates. It is the precondition U-38 and U-39 each name. **The expensive half is paid, 2026-08-30 (D-0039).** First grading: 16/24, five passes earned by declining to answer, and every failure filed by an `explain --proposals` run rather than a verdict read — five to U-23, one each to U-38, U-40 and U-41, the last two registered by this measurement. The suite's lifecycle trap caught the ingest leaving three superseded proposals governing (one retirement per promotion; a trusted `Superseded-By` header) and the fix bought a pass without the question moving. What resolution does not close: U-11 still fires at runtime (authors' names enter the ledger, never the repository), and the questions were agreed by one hand against a corpus that hand chose — a reviewer who disagrees with an expectation should re-agree the row, not trust it. |
| U-10 | ~~Incremental projection maintenance~~ **Resolved 2026-08-23** → D-0016: the index carries no view parameters and nothing is removed from it, so `rebuild` *is* `empty().advance()` — equivalence is definitional. Backed by proptest properties (incremental == rebuild, advance idempotent, index state == ledger state, views never mutate). | — | The residual question is performance, not correctness: buckets accumulate dead slots. Tracked as U-18. |
| U-11 | ~~Redaction vs append-only: a designed legal "remove this" that preserves chain integrity~~ **Resolved 2026-08-31** → D-0047: the declaration is an appended record (human-only, target checked, ground required, permanent), and the removal is `redact_store` — a rewrite that husks the withheld fields, stamps a receipt naming the declaration plus a fingerprint of what stood there, and must replay through the full grammar before it may replace the log | — | The trigger fired 2026-08-29 when the proposals reader put real authors' names into ledgers at runtime, and sat fired for two days — which is itself the lesson: a trigger nobody re-reads is a wish, and the quarterly re-read exists for exactly this row. The receipt is what keeps the door lawful: a store refuses to open when a husk's mark names no redaction of it, and the load path is the only source of husks, so "redacted" is not a word anyone may write over anything. Residuals stated in D-0047 rather than implied: bytes on old media and copies are the operator's duty (crypto-shredding is the mechanical shape if wanted), the fingerprint matches rather than proves, and what the scope does not reach is U-46. |
| U-12 | ~~ID & dedup: ULID vs content-addressing; exact-dup grammar vs semantic-dup keeper policy~~ **Resolved 2026-08-31** → D-0051: ULIDs for records and fingerprints for documents were already practice (D-0019, D-0021); a byte-identical re-proposal is a witness — appended legally, disclosed at the tool, folded in the inbox behind its first copy with the pairing published for set verdicts | — | The trigger fired 2026-08-23 and sat fired for eight days — the second row this week to do it, which makes the quarterly re-read look generous and is noted for that re-read to find. The three moves are all precedent: disclosure is D-0048's shape, folding is D-0024's, and one-verdict-many-records is D-0034's. What identity cannot see is drawn plainly: byte-identity only — the paraphrase half belongs to the keeper, needs meaning, and waits where U-23 waits (D-0045 measured why). G-23 was re-agreed in the same change, reworded per D-0023's discipline, because its abstention cited this row. |
| U-13 | Envelope schema evolution: versioning + migration policy | First needed envelope change | Records carry `envelope_version` from day one. First real precedent arrived 2026-08-24 and was *content*, not envelope: D-0023 added a field to a verdict action, and the migration answer was a read-only value (`unstated`) that names the absence rather than a default that invents a meaning. Whether `envelope_version` should also gate content shape is the open half. |
| U-14 | ~~Bitemporal edge cases: corrections-of-corrections, overlap, precision~~ **Resolved 2026-08-24** → D-0036: a reference semantics transcribed from §3.1, eight properties over generated ledgers, and the corrections and overlap cases written out as named tests | — | It found one: a view over a projection that had not been advanced reported a retired claim as promoted when asked about *now*, with the current ledger in the same call. Fixed by falling back to the fold — slower and right. Worth recording how it was found: six properties passed on the first run, and they were the ones that tied both axes to one instant and used a freshly advanced index. The defect appeared the moment a property held an index while the world moved on. Overlap gained a past tense as `contradictions_at`, which was the only read in the engine without one. |
| U-15 | Contradiction detection scope: exact-scope in engine (invariant 7), semantic in keeper | Retrieval implementation | Where grammar ends and meaning begins. v1 core implements exact scope over *attributes* only — relation-predicate cardinality ("can `reports_to` hold twice?") is semantics the engine cannot know, so it stays keeper-side. |
| U-16 | ~~Set verdicts: bulk mechanical ingest cannot take per-record human verdicts~~ **Resolved 2026-08-24** → D-0034: a verdict may name a set and must say on what footing one person speaks for it — an ingestion run, one editorial act, or a set read in full | — | Invariant 5 untouched: a human still declares it, an agent still cannot, and there is a test. The targets are enumerated rather than named by a run, forced by the state fold being a pure function of the action — the second time that property has chosen an interface. Measured: a generated sync of two thousand rows takes one verdict instead of two thousand. The staged-evaluation idea in the original note is still unexamined and belongs with the keeper layer. |
| U-17 | ~~Engine license: Apache-2.0 vs MIT vs dual~~ **Resolved 2026-08-31** → D-0050: dual, MIT OR Apache-2.0 at the user's option — the Rust convention, the patent grant, and a trademark exclusion that quietly serves U-6 | — | The reasoning sat on this row for eight days before the owner declared it: permissive because fork-safety answers single-vendor fragility, dual because it buys simplicity and the patent grant at once. The crates stay `publish = false` — the source being free and crates being pushed under a name counsel has not reviewed are different questions, and the second is U-6's. G-10 was re-agreed in the same breath, because resolving this row made the suite's canonical abstention unsatisfiable — the exact shape the stale-trigger check was built for, caught by design this time rather than by accident. |
| U-18 | Candidate-index compaction: dead slots accumulate because nothing is ever removed from the fold | When a corpus makes the scan cost measurable | The accepted cost of U-10's monotonicity. **Looked for at 68,000 records 2026-08-24 (D-0030) and not yet uncomfortable:** projection rebuild and contradiction detection are both linear and both in milliseconds. Kept open because the generated corpus retires and supersedes at a fixed rate rather than a realistic one, so this has been measured under kind conditions. |
| U-19 | ~~Ingest idempotency~~ **Resolved 2026-08-24** → D-0021: the documents are upstream and a re-ingest is a sync — each source record fingerprinted into its own provenance, so unchanged records write nothing and edited ones supersede what they replace | — | The trigger fired the day durable storage shipped and was not noticed for a session. The interim answer ("ingest into a fresh ledger") had quietly become the opposite failure: the host refused to re-read, so the upstream copy was upstream in name only. Content-addressing per U-12 turned out to be needed at the *document record* level, not the ledger record level. |
| U-20 | ~~Set verdicts vs the transcription cost: two verdicts per record for what a person performed as one editorial act~~ **Resolved 2026-08-24** → D-0034: one verdict, on the footing of one act | — | The small version of U-16 and settled by the same grammar. The corpus now holds thirty-three transcribed verdicts where it held sixty-six. The keeper split each record into a claim and a title because the model wanted them apart, and was charging that split back to the author as a second declaration. |
| U-21 | Relation-scope contradictions never surface (invariant 7 covers attributes only) | With U-15 | Raised by adversarial review 2026-08-23 and confirmed as real-but-deliberate: the engine cannot know a predicate's cardinality. Kept visible so it is a decision, not an oversight. |
| U-23 | Retrieval quality: the ranker matches words rather than meaning, so two phrasings of one question do not meet | Before any claim that retrieval is good | **Narrowed 2026-08-23 (D-0020):** the plumbing is done — `Embedder` trait, `VectorIndex`, two rankers through fusion. Its top-hit similarity spans 0.49–0.66 on answerable questions and 0.47–0.60 on unanswerable ones, overlapping, so vector similarity may raise an offer and never confer confidence. **Narrowed again 2026-08-24 (D-0028), and "what remains is a model" was three-quarters wrong.** Measuring found four faults ahead of the model and fixed three: gaps were taking answer slots *and setting the confidence of the answers beside them*; `keys` could not reach `key`; and coverage was answering two questions at once, now split into coverage and reach — reach separates the suite cleanly, every unanswerable question at or below 0.32 and every answerable one at or above 0.52. Guessing lost to measuring more than once: sweeping the fusion constant across six values moved the score by nothing. The relaxation that came with the split was measured and refused — it buys one answer and costs one abstention, and no threshold separates the two cases. ~~What is left genuinely needs meaning: G-08, G-09 and G-13 each ask in words the answering record does not use.~~ **Corrected 2026-08-29 — see the end of this row.** Named by id deliberately — restating them here is what U-27 warns about, and this row did it once already. **Moved without being touched, 2026-08-29.** G-13 went from UNDERCONFIDENT to MISSED when D-0038 entered DECISIONS.md. Nothing in the question changed and nothing in D-0015 changed: its anchor simply fell from third place to fourth, displaced by D-0034. Isolated by rolling each corpus file back on its own — DECISIONS.md restores the earlier verdict, REGISTER.md changes nothing — so a single new decision record re-sorted it. Why it is that fragile is visible in `explain`: three of the question's content terms have no posting anywhere in the corpus, so its ranking rests on the ordinary words left over, and those shift whenever the corpus grows. The suite called zero regressions and was right to: G-13 was already a known shortfall against this entry, so the move was between two failure classes and nothing watches that boundary. D-0037 catches a question whose agreed words the corpus acquires; it does not catch one an unrelated addition re-sorts, and at a record a session the second is now the commoner event. Written without restating the three terms, for the reason this row already gives. **The list above was wrong in both directions, 2026-08-29.** Measured with `explain` rather than read off the verdicts. G-09 does not ask in words its answering record avoids: reach is 1.00 and D-0019 uses all seven of the question's terms. It loses to the *title* claims of D-0006 and D-0012, short enough that the two discriminating terms dominate them — a ranking fault, and the ingest's own split of each record into a title and a body is what supplies the competitor. G-19 belongs on the list and was never on it: its answering record uses two of six content terms, while the corpus's single posting of the rarest sits in D-0024 and the next rarest in D-0023, so the terms that should discriminate point away from the answer. G-13 stays, on reach 0.28. G-08 is a fourth case again and is now U-38. Three of these four were tracked under a cause that does not explain them — which is what a register is for, and also what it is vulnerable to: the row was written once and re-read as fact afterwards. **First measured on real language, 2026-08-30 (D-0039).** Five of the P-suite's eight failures land here, each with its mechanism in the explain output: an answer at vector rank 0 and lexical rank 8 that fusion cannot save (P-02), same-vocabulary neighbours 0.02 and 4% apart (P-17, P-03), and the calibration case twice — right record on top, coverage 0.31 and 0.42, declined (P-09, P-16). Also the first corpus where the second ranker earns something the suite can measure: vector candidates move four questions from failing to passing, which reverses what U-33 recorded about the self-corpus and is evidence the ranker was never useless, only unmeasurable at 54 records. **Three of the five were the budget, not the ranker, 2026-08-30 (D-0041).** P-02, P-03 and P-17 recovered when assembly widened from one document to k: their answers sat at fused ranks one and two all along, and the tight margins their filing rested on were real measurements of the wrong layer. What this row now holds on the outside corpus is the calibration family — P-09, P-12, P-16: right record surfaced, coverage 0.31 to 0.48, declined — the confidence half of D-0020, and no longer the ranking half at all. **P-22 joins them, 2026-08-30 (D-0042):** with U-38 refused, its bluff is this row's purest case — every word of the question spoken by a corpus that does not settle it, and no lexical quantity to tell that from an answer; the calibration table shows it reading identically to questions that must answer. All four wait on meaning, which is what this row has said since D-0020. **"Waits on meaning" was then measured, 2026-08-30 (D-0045), and was half wrong.** A real embedding model behind the trait built for one moves the proposals suite net zero, costs the self-corpus one, and its answerable/unanswerable similarity ranges overlap on both corpora just as the stand-in's did. The reason is structural and this row should have seen it: five of the eight remaining shortfalls are confidence-shaped, confidence is lexical by design (D-0020, re-confirmed), so no model reaches them through the vector channel; the reachable rest trade one-for-one, and one loss is a question the stand-in's character n-grams were rescuing. What this row waits on now, stated precisely: either a model-and-plumbing whose similarity *separates* (the caps are U-45), or a way to read meaning inside the confidence rule — and the second is licensed only by the first. **The caps were then lifted and the overlap stands (D-0046):** prefix applied, whole document voiced, record answering as its best window, on both corpora — the register stops predicting the vector channel will fund confidence at this model scale, and what would reopen the question is a materially larger model or a corpus whose questions hide more meaning than these do, not more plumbing. |
| U-33 | ~~The corpus is written in one dialect and questioned in another~~ **Resolved 2026-08-24** → D-0029: a term the index has no posting for is read as one interior edit away, when both words are five letters or more; the substitution is published, and two candidates mean it answers neither | — | The rule that the edit must fall *inside* the word was the whole of it: an edit at the end is a suffix, and without that guard the suite produced one false neighbour that cost a question the answer it had already found. Unforeseen consequence worth recording: this took over the job D-0020 gave the vector ranker, and did it better, because a lexical bridge counts toward coverage where a close vector is only ever an offer. On this corpus the second ranker now earns nothing the suite can measure. |
| U-34 | Reading a term as its near neighbour scans the whole vocabulary, once per query term that reaches nothing | When a corpus makes the scan measurable | The accepted cost of D-0029, and the same shape as U-18 and U-24. Bounded by unmatched terms rather than by query length, so a question the corpus can answer pays nothing at all. The fix has a known shape when it is wanted — a deletion index, or a BK-tree over the vocabulary — and pre-building either would be exactly the optimisation this register keeps declining to make early. |
| U-35 | ~~Retrieval's cost is the per-candidate admission fold~~ **Resolved 2026-08-24** → D-0031: it is not. Admission is 5.2ms and the arithmetic 4.9ms of what was a 42ms query; the cost was the gap channel recomputing both candidate lists, so every query ran the vector scan twice | — | Registered by reading the code and refuted by timing it — the third guess this week that measurement overturned. The duplicate was unconditional and always had been: the gap path cleared an entity scope in the only branch reached when there is no entity scope. Sharing the answer path's candidates: 42ms to 29ms at 68,000 records, no change in what comes back. |
| U-36 | ~~Neighbourhood buckets are built and kept for a facility that is off by default~~ **Resolved 2026-08-24** → D-0033: they are opt-in at construction, a probe asked of an index without them falls back to scanning, and every caller that exists takes the plain index | — | Measured properly in one run rather than remembered from two: 2.02s against 2.50s to build 35,480 vectors, and 4.5 MB of bucketed ids — about 128 bytes of index per vector. The register's estimate was close, which is worth noting given how many of this week's were not. One thing the change nearly took with it: making the buckets opt-in silently reverted the property test that had just been added to cover them, because the default it used had moved underneath it. |
| U-24 | Snapshots and compaction: replay is O(log) on every open, and the log only grows | When open time or log size becomes uncomfortable | The accepted cost of D-0019. A snapshot must itself be replay-validated or it reintroduces the bypass it exists to avoid — that is the design constraint, not the file format. |
| U-25 | `sync_data` per append is not slow, it is a different regime | When a bulk corpus must be ingested at all | **Measured 2026-08-24 (D-0030):** 2µs per record in memory, 4,095µs durable — a factor of two thousand. 4,365 records take eighteen seconds; a hundred thousand would take most of an hour. The register said "correct but slow", which understated it badly enough to be worth recording as a lesson about adjectives. Batching still needs a durability story for the batch boundary: what a caller is promised when a batch is half-written. Interacts with U-16's set verdicts. |
| U-26 | ~~Approximate nearest neighbours: vector search is an exact scan~~ **Narrowed 2026-08-24** → D-0032: the index carries predicate-aware neighbourhoods and a query can walk them instead of reading everything — built, measured, and left switched off | When a better index beats 17% of the scan for 65% recall@10 | Random projection rather than a navigable graph, chosen by the invariant: a signature depends on its own vector, so incremental still equals rebuild (D-0016), and the property test now covers the vector index. It does not earn the default — two and a half times faster for a third of the recall, on a curve that flattens. The fault is the method and not the data: the best match sits 115% above the median, so the neighbourhoods are real and LSH just needs a bigger corpus than this. HNSW remains the registered shape, and now has a bar to clear. |
| U-27 | ~~Corpus self-reference distorts its own measurement~~ **Resolved 2026-08-24** → D-0037: every question records the words of it the corpus did not contain, and the suite turns red when one arrives — which with D-0029's phrase check covers both ways a self-describing corpus moves its own measurement | — | D-0029 said the vocabulary half could not be mechanised and that was half right: checking the *state* is hopeless (a rule over rare words fires thirteen times here, mostly on a corpus legitimately holding its own topic), while checking the *change* is precise, because a word arriving is an event with a date. It fires on innocent records too, correctly — a question chosen because the record was silent is not the same question once the record speaks. The structural answer is still a corpus the record does not describe (U-9), unused because synthetic prose cannot grade real language. |
| U-28 | ~~The verdict grammar has no supersession path for a gap or a hypothesis~~ **Resolved 2026-08-24** → D-0023: withdrawal carries a reason (superseded, answered elsewhere, no longer relevant, registered in error), a hypothesis gains `abandoned`, and `supersedes` is enforced same-kind | — | The registered shape of the fix was the right one and was followed. Two things it turned up that were not foreseen: the corpus had *already* been recording "resolved, and this ledger does not hold the answer" as plain withdrawal, which is a provenance alarm that had no way to sound; and the append-path-or-grammar distinction decided where a check belongs for the second time in two days (D-0022 was the first), which makes it a rule rather than a coincidence. |
| U-31 | ~~Whose signature counts~~ **Resolved 2026-08-24** → D-0026: only a key git says this machine trusts (`%G?` = `G`, not `U`), and when the caller names signers, only an identity the signature itself binds — named from outside the repository, because a list of who may promote cannot live in the file it protects | — | Half of it turned out to be already answered and thrown away: D-0025 had collapsed `G` and `U`, which accepts a key an agent minted a second earlier. The endpoint limit the entry predicted stands exactly as predicted, and is stated in D-0026 rather than papered over. What the work added that the entry did not foresee is U-32. |
| U-32 | ~~Trust is a history, not a current view~~ **Resolved 2026-08-24** → D-0027: the recorded attestation stays immutable and `review_trust` re-asks the repository on demand, sorting every promotion into verifying-as-it-did, weakened, strengthened, unverifiable, or naming no commit | — | Both readings now exist and neither replaces the other. Two things the entry did not foresee: *strengthened* is as real as weakened and needed its own column, or the review would read as an error report rather than a measurement; and the review had to be a read and never a write, because a revoked key is not a verdict and no person declared one. The predicted cost holds exactly — an unreachable commit is `unverifiable`, which is a third answer and not a failure. |
| U-30 | ~~A superseded claim that was never promoted is never closed~~ **Resolved 2026-08-24** → D-0024: it stays proposed, correctly — nobody ruled on it — and stops being a *separate* entry in the inbox. `pending_proposals` returns the head of each supersession chain plus the drafts folded behind it, both lists present | — | The fault was in the queue, not the state. Worth recording what was nearly done instead: transcribing a rejection for the replaced draft. It would have looked unremarkable beside the ingest's other transcribed verdicts, and it would have been a human verdict for a record no human read. |
| U-29 | ~~The upstream document is an unaudited promotion channel~~ **Narrowed 2026-08-24** → D-0025: every transcribed verdict now carries what git can establish about who wrote the words asserting it, and `--require-signature` declines to transcribe one that no signed commit carries | Before an agent is given write access to the corpus | Narrowed, not closed, and the residue is named as U-31. The channel is no longer unaudited: a promotion backed by nothing says so in its own author detail, and `unattested_promotions` will list every one of them at any later time. What is still true is that git attests the typist and not the intent, and that this keeper takes git's verdict on a signature without knowing which keys the project trusts. |
| U-22 | ~~A backwards system-clock step makes every `append` fail with no recovery path~~ **Resolved 2026-08-24** → D-0022: small steps hold record-time at the last entry, large ones refuse and name the moment appends resume, and the future-time check moves to the append path so replay stops applying it | — | Understated as registered. Once the log outlived the process, a clock set backwards did not block the next few writes — it made the store refuse to open at all, every record in it now reading as future-dated. Found by a test written for the smaller problem. |
| U-37 | Ranking drift: a question that passes can be re-sorted by a record with no bearing on it, and nothing detects the move | When zero regressions is leaned on as a gate rather than read as a score; or if a corpus is ever synced from a source that keeps writing — re-read 2026-08-31 (D-0052): the outside corpus D-0039 built is pinned by commit, so it cannot drift under this row | Raised by G-13 on 2026-08-29, and visible only because it was already failing. It crossed from one failure class to another when D-0038 arrived — a record with nothing to say about it — and the suite counted zero regressions, correctly, since a known shortfall staying a known shortfall is not a regression. The same move under a passing question would have been exactly as silent, and that is the entry: the suite grades an outcome per question and reports a total, so it can see a verdict get worse but not an anchor shift beneath one that still passes. D-0037 covers the case where a question's agreed words arrive in the corpus, because a word arriving is an event with a date. This is the other direction — the words hold still and the weights move under them — and it has no event to hang on. The shape of a fix is a per-question note of what answered it and at what rank, compared run to run, which is a baseline of a moving quantity and therefore precisely what the vocabulary baseline was designed not to be. Declining to build it while the corpus is one hand writing a record a session is deliberate: the alarm would fire on every commit, and an alarm nobody reads is the failure this register has already named twice. **The passing-question case arrived 2026-08-30 and was caught, for the narrow reason this entry allows:** D-0039's arrival moved G-07 all the way across a verdict boundary, and a crossed boundary is the one move the suite does see. The entry stands for what stopped short of one. |
| U-38 | ~~Whether a record that matched everything the corpus can answer should be allowed to answer confidently, however little that was~~ **Resolved 2026-08-30** → D-0042: refused, on the outside measurement the trigger demanded — a bluff at margin 1.02 beside an honest answer at 1.01, the motivating question drifted out of the rule's own precondition, and two questions with identical readings needing opposite outcomes | — | Raised by G-08 on 2026-08-29, and it is neither a ranking fault nor a vocabulary one in the usual direction. The answering record is found at rank 0, four times clear of the next distinct record, and coverage equals reach exactly: it matched the whole of what this corpus is able to match. What declines it is the confidence rule, reading an absolute coverage that no record could have beaten. The ratio of coverage to reach reaches 1.00 on four of the suite's twenty-one questions — three are already confident passes and the fourth is G-08 — while every question that ought to abstain sits between 0.00 and 0.46. So a rule of the shape 'matched all that was answerable, and by a decisive margin' looks like it would buy G-08 without spending an abstention. Recorded as an idea and not a result: D-0028 measured one relaxation and refused it on exactly this shape of reasoning, twenty-one questions is a small sample, and a suite grading a corpus it also describes is what U-27 and U-9 both warn about. It needs implementing and measuring, and the margin half of the rule is unspecified. **A second fault in the same rule, 2026-08-29.** Confidence is lexical by design (D-0020), but the record it is read from is the first assembled item and items sit in fused order, so the second ranker chooses whose confidence counts. Nothing here shows it; it decided two questions in every U-39 variant. Needs U-9. **Closed 2026-08-30 (D-0042):** the corpus the trigger demanded existed for a day before it settled this — refused, on the three exhibits in the decision record, read off the calibration instrument. The second fault outlived the rule it was filed under and moves to U-44. **The outside measurement exists, 2026-08-30 (D-0039).** P-22 is what the margin half was waiting for: coverage equals reach at 1.00 on a question the slice does not settle — the corpus speaks every word of it and answers none of it — and the current rule bluffs confidently. The lexical margin over the runner-up is 1.6%, so the proposed rule survives its first outside test *only if* the margin clause is load-bearing; the unspecified half is now the whole question. The ratio itself separates nothing on real language: every answerable question and half the abstentions sit at reach 1.00, and the six questions where coverage *equals* reach at 1.00 are three passes, two wrong anchors and the bluff — where the self-corpus's four were three passes and G-08. |
| U-39 | ~~Ranking is decided by document length: every record is indexed as a short title and a long body, and the length constant chooses which of the two can win~~ **Resolved 2026-08-30** → D-0044: no longer true of the current engine — the B sweep is flat on both corpora, the 2026-08-29 one-for-one trade dissolved under D-0040 and D-0041, and the predicted repair (passage indexing) was built, swept at six sizes over both suites, refused everywhere, and kept switched off behind `with_passage_tokens` per the U-26 pattern | — | Raised by G-09 on 2026-08-29 and measured rather than argued. The index holds 202 documents for 54 records — a title of about ten tokens, a body of 95 to 351, and one document per mention edge whose entire text is a single word. 88 documents are five tokens or fewer and 132 of 202 fall under half the average, so the average that length is discounted against is 79.5 where the prose alone averages 140.1. One occurrence of a term worth 3.17 earns 5.32 in a one-token edge, 4.94 in a title, 2.94 in a 95-token body and 1.32 in D-0019's 351-token body: the record G-09 wants is the most discounted document in the comparison, and its title carries neither of the question's two discriminating terms while D-0006's and D-0012's titles carry one each. The obvious repair was measured and falls short — setting the edge records aside raises the average to 140.1 and moves that body from 1.32 to 1.96, still far behind a title at 5.12. Sweeping the constant across five values is the real result and it is negative: at 0.75 and 1.00 the suite scores 17 of 21 with no regression; at 0.50 and 0.25 G-09 passes and G-07 breaks, one for one, which is strictly worse because it trades a claimed shortfall for an unregistered regression; at 0.00 G-03 breaks too and the score falls to 16. What the sweep exposes is that the constant is not a quality dial. At 0.75 a record whose title matches takes the top two lexical places with its title and its body together; lowering it breaks that pair apart, so D-0011 keeps first place and loses second while D-0019 climbs from eighteenth to ninth. The constant selects which kind of question can be answered — those answered in a title, or those answered in a body — and this suite holds one of each. A repair worth the name would change how a record is indexed, not how length is discounted. **The corpus the trigger asks for exists, 2026-08-30 (D-0039):** titles average 7 tokens against bodies of 3,683 — twenty-six times the skew the constant was swept on. The sweep has not been rerun there; before any move on BM25_B it must be. **A second length effect joined it, 2026-08-30 (D-0043):** coverage — unlike BM25 — carries no length discount at all, so the longest document covers the most of any question, which is what made "judge the best coverage among items" manufacture a bluff and is one more way this row's repair-worth-the-name (change how a record is indexed) would pay twice. |
| U-40 | ~~A question about a refusal cannot be answered from the governed view: refused and retired records are invisible to the default retrieval plan, and nothing routes such a question to a wider one~~ **Resolved 2026-08-31** → D-0048: the view is a parameter the asker holds (`full_history` serves the forensic view, every record labeled), and the engine discloses the strongest record its view refused whenever its own outcome is less than confident — same pass, same bars, never an answer | — | Registered by P-08, 2026-08-30 (D-0039). The answering record is in the ledger and in the index and absent from both rankers' candidate lists, because its claims are Rejected-state and the default view admits promoted only. This is design/001 §7 behaving as written and still a wrong outcome for a fair question — the view is a parameter the asker never chose. The forensic `StateFilter::All` exists; what is open is who decides a question is *about* a record rather than answered *by* it, and that distinction is semantics, which is why this row waited for U-3 rather than proposing a heuristic. **The resolution kept that refusal (D-0048):** nothing routes, nothing guesses — the asker holds the view parameter, and the disclosure tells them when holding it differently would have mattered. The scan was already reading the refused postings and dropping them silently, so the disclosure costs one branch in a pass already paid for. P-08 recovers as `pointed+beyond`, the suite's verdict for a correct pointer that is deliberately not an answer; the governed plan itself did not move on either corpus, which is what "the ratchet holds" means as a number. |
| U-41 | ~~Fusion can lose a record both rankers found~~ **Resolved 2026-08-30** → D-0040: reciprocal rank fusion with k=0 — first place in either list is evidence and depth is not; swept over both suites, recovering G-07 and moving nothing else | — | Registered by P-12 (D-0039), reproduced same-day by G-07 and G-10 when this row's own arrival re-sorted the self-corpus. The resolution corrected the entry twice, which is worth keeping: G-10 was never fusion's casualty — its mechanism was the gap channel's own ranking (U-42), and the claim that the gap channel "reads the same fused candidates" was written from memory of D-0031 rather than from the code. And P-12, the founding evidence, was not fusion's either — under k=0 its answer sits at fused rank 2 and the token budget assembles one document, which is U-43. Of the three cases this row ever held, one was fusion. D-0028's "sweeping the constant moved nothing" was about the corpus, not the constant: on today's corpora, k moves exactly one question, in one direction, on one suite. Score-normalized fusion was measured and refused — it costs all four vector rescues. What k=0 does not claim: a first place *deserved* — the two-champion case where a wrong record leads each list is a ranking fault and stays with U-23. |
| U-42 | ~~Gap offers ranked by closeness over coverage: the one gap covering the question sat behind three sharing no words with it~~ **Resolved 2026-08-30** → D-0040: coverage ranks; closeness opens the door and breaks ties, never decides the order | — | Found by G-10 with the budget at three and the covering gap at rank four; measured by shutting the closeness entry path (only the covering gap remained) before touching the sort. The fix is the asymmetry the answer path already gives similarity — good enough to raise an offer, never to outrank a lexical match — applied to the offers themselves, and it was the gap channel's own comment's stated rule all along. The residue belongs to the embedder, not the channel: a real embedding model's closeness might deserve rank weight, and that is U-23's model half, not a new row. |
| U-43 | ~~On a long-document corpus the token budget, not the ranker, decides how many answers a consumer can see~~ **Resolved 2026-08-30** → D-0041: a record that would not fit its share of the budget is excerpted to the window covering the most of the question, the share being the budget's own arithmetic (`max_tokens / k`) rather than a new constant | — | Registered by P-12 (D-0040) when 37 of 38 fused items were cut and a rank-2 answer graded as never surfaced. The repair moved the P-suite 16/24 to 19/24 with the self-corpus standing still — and the three recoveries were the finding: P-02, P-03 and P-17 had been filed under U-23 as meaning faults, on real explain evidence of tight margins, while their answers sat at fused ranks one and two behind a one-item assembly. Third correction of cause in two days, and the second time the *instrument* was the accomplice — explain conflated fused order with assembly until D-0040 split them, so even measurement-based filing inherited it. Residuals stated in D-0041: excerpt readability is ungraded, and the ranking half of long documents is U-39's, untouched. |
| U-44 | ~~Whose coverage is the outcome's: it is judged from the first assembled item, and fused order chooses that item~~ **Resolved 2026-08-30** → D-0043: per-item coverage is published everywhere (adopted half), and judging the best among assembled items is refused (measured half) — the record covering the most words of an unanswerable question is simply the longest one, so "best" manufactures confidence out of document length | — | The exposure the row asked for settled it in one sitting. On the self-corpus, reading the best instead of the first changes nothing; on the proposals corpus, "best" buys three passes — two of them from a *wrong* record's coverage vouching while the right one sits adjacent — and spends one honest abstention on the suite's costliest failure, via a 10,815-token document that covers all of a question about upload sizes while answering none of it. P-12 was the legitimate case (its own record covers 0.71 at rank three) and nothing mechanical separates it from the bluff, which covers more. Same closing shape as U-38: the columns do not contain the answer, a consumer with meaning might, so the number is published per item and not decided for anyone. Found on the way: `Retrieved::coverage` documented "best item" while the code read the first — the second contract-against-code gap this week (U-42 was the first), both found by instruments rather than review. |
| U-45 | ~~The embedding plumbing caps any model put behind it: a document is embedded by at most the model's context window, and the one `embed` method cannot apply the asymmetric query prefix retrieval models prefer~~ **Resolved 2026-08-30** → D-0046: both caps lifted (`embed_query` on the trait, embedding windows on the vector index) and the ceiling did not move — the similarity overlap was never the plumbing's | — | Registered by D-0045, where a real model's similarity ranges overlapped for possibly these reasons and possibly its size — the measurement cannot tell, which is the point of naming the caps. The shapes of the lifts are known and unbuilt: passage-level embedding can reuse the passage machinery D-0044 left switched off (embed windows, score a record as its best window's similarity — the vector-side twin of what the lexical sweep refused, and it may fare differently because an embedding has no term frequency to dilute); and the trait can grow an `embed_query` with a default falling through to `embed`, so the stand-in never notices. Both were then built and measured (D-0046): the prefix changes nothing, and the windows reproduce the length lottery in vector space — the longest document's fifteen windows are fifteen chances, and P-23's abstention became a bluff a third time. The lifts stay (the seam is correct plumbing; the window door parallels the passage door, off); the overlap stands in every configuration. |
| U-46 | ~~Redaction reaches records only: an entity's label and a source reference are outside its scope~~ **Resolved 2026-08-31** → D-0053: a declaration now targets an entity (label husked, receipt on the entity, forgery refused at open) and a `Source` scope withholds the envelope's reference while the channel stays | — | Registered by D-0047 as the stated edge of the mechanism rather than a gap discovered later. Entities are structural — labels thread through the projection, retrieval scopes, and every "about" listing — so husking one is a wider rewrite than a record's, and the proposals corpus does not force it today: authors live in envelopes there, which D-0047 covers. The shape when wanted was the same two halves — declare against the entity, rewrite label and log together, prove by replay — and that is the shape that shipped, ahead of the trigger on the owner's direction and U-11's preparedness argument (the reasoning is in D-0053, including why that is not the ahead-of-evidence building D-0049 refused). Residue worth one line: a husked entity is unfindable by label, so re-syncing the same upstream mints a fresh entity — re-importing removed data is an operator's act, and the store will not quietly reunite it. |

## Room 3 · Unknown knowns

Knowledge in the builder's head but not yet in the record. The founding interview
extracted two — proof the practice works:

- The write-path omission (→ D-0006, U-1): a load-bearing architectural question
  that had never been consciously decided.
- The scar-tissue requirements (→ REQUIREMENTS.md): production lessons that existed
  only as workarounds in application code until asked for.
- The two-ledger tension (→ D-0013, data-model round 2026-08-23): R-5's
  cheaply-mutable weights and D-0004's append-only assertions contradicted each
  other in the founding records themselves — unnoticed until the phase interview
  forced the question. The register audits its own known knowns.

**Standing practice:** an interview-before-phase. Before each new phase (data model,
storage, retrieval, serving), run the veteran question against the builder: *"what
are you already sure of here that is written nowhere?"* Capture answers as decision
records with envelopes.

## Room 4 · Unknown unknowns

Nothing can be listed here — that is the point. What exists instead are practices
that convert surprises into registered entries while they are cheap:

- **Blind-spot pass before each phase.** State experience level honestly, then ask:
  what would someone who has built storage engines / query planners / vector indexes
  inspect, challenge, or measure here? File the answers as U-entries.
- **The priors survey** ([priors/](priors/)) — walking through other builders'
  known-known rooms before trusting this project's map. Reference-class thinking:
  what killed or stalled the engines that came before.
- **Quarterly register re-read.** The backward door is real: promoted decisions go
  stale while the record keeps speaking in the present tense. Every decision's
  review trigger gets checked; anything overtaken by events is retired, not edited
  in place. **Partly mechanical since 2026-08-31 (D-0052):** any trigger that
  names a question the register has since resolved turns the build red until
  the row or record is re-read and its trigger reworded to acknowledge — naming
  the resolving decision, never the resolved question. Prose-only triggers
  remain the re-read's own work; the first mechanical pass found fifteen in
  arrears, which is what the quarterly cadence was quietly accumulating.
- **Golden suite that rewards abstention** (H-0001c). A fluent wrong answer from the
  system is a drift alarm, not just a bug.
- **Pre-publication scrub pass.** Before the repo goes public: run
  `scripts/check-boundary.sh` — the D-0010 boundary as an executable rule
  rather than a remembered one, now covering who the commits say made the work
  as well as what the files say (D-0035) — then check for private-path
  references that leak context, and confirm U-6 and U-7 are resolved. The script matches one
  name case-sensitively because it collides with an ordinary English word.
  That nuance exists because the naive rule cried wolf four times in a single
  day, and an alarm nobody reads is worse than no alarm at all. **Amended
  2026-09-03 (D-0054):** the first run over *every* tracked file found the
  names inside the script itself, where they had been since the day it was
  written. The patterns now live outside the tree and the script refuses to
  call an empty search clean; history was rewritten to remove them on
  D-0038's terms, with the pre-rewrite record preserved at
  `../tacit-backup-20260903.git` (head recorded below once the mirror
  existed). The scrub's default targets now include itself.

---

*Recorded 2026-08-23, amended 2026-08-24. First scheduled re-read: 2026-11-23.
Owner: Greg Villa.*
